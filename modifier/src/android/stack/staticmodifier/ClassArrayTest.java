package android.stack.staticmodifier;

public class ClassArrayTest {
    public static void main(String[] args){
        /**
         * 这是由虚拟机自动生成的、直接继承于java.lang.Object的子类，创建动作由字节码指令newarray触发。
         */
        Farther[] farthers = new Farther[10];
    }
}
